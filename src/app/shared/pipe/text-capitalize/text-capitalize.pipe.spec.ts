import { TextCapitalizePipe } from './text-capitalize.pipe';

describe('TextCapitalizePipe', () => {
  it('create an instance', () => {
    const pipe = new TextCapitalizePipe();
    expect(pipe).toBeTruthy();
  });
});
